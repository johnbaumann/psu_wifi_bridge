#pragma once

#include <driver/gpio.h>

extern const gpio_num_t kPin_DSR; // IN
extern const gpio_num_t kPin_CTS; // IN
extern const gpio_num_t kPin_DTR; // OUT
extern const gpio_num_t kPin_RTS; // OUT